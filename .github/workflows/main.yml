name: Kerdy Dev Server CI/CD

on:
  push:
    branches: ["Feature/#37-자동배포_구현"]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: deploy
      run: |
        ~/deploy.sh